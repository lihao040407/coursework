位置5：结果显示 - 流程图
============================================

比赛结束，所有线程join完成
    ↓
┌─────────────────────────────────────┐
│ 打印标题                             │
│ "TEAM RESULTS"                       │
└─────────────────────────────────────┘
    ↓
┌─────────────────────────────────────┐
│ 遍历所有队伍（i = 0 到 3）          │
│ for (int i = 0; i < NUM_TEAMS; ++i)│
└─────────────────────────────────────┘
    ↓
    ├─ 队伍0（Jamaica）
    │   ↓
    │   ┌─────────────────────────────┐
    │   │ 打印成绩                     │
    │   │ Team Jamaica = 41.5 s        │
    │   └─────────────────────────────┘
    │   ↓
    │   ┌─────────────────────────────┐
    │   │ 检查是否被取消资格           │
    │   │ if (teamDisqualified[0]) {  │
    │   │     打印取消信息             │
    │   │ }                            │
    │   └─────────────────────────────┘
    │
    ├─ 队伍1（United States）
    │   ↓
    │   ┌─────────────────────────────┐
    │   │ 打印成绩                     │
    │   │ Team United States = 0 s     │
    │   └─────────────────────────────┘
    │   ↓
    │   ┌─────────────────────────────┐
    │   │ 检查是否被取消资格           │
    │   │ if (teamDisqualified[1]) {  │
    │   │     打印取消信息 ✅          │
    │   │     "*** Team United States  │
    │   │      was DISQUALIFIED..."    │
    │   │ }                            │
    │   └─────────────────────────────┘
    │
    ├─ 队伍2（Great Britain）
    │   ↓
    │   ┌─────────────────────────────┐
    │   │ 打印成绩                     │
    │   │ Team Great Britain = 41.8 s  │
    │   └─────────────────────────────┘
    │   ↓
    │   ┌─────────────────────────────┐
    │   │ 检查是否被取消资格           │
    │   │ if (teamDisqualified[2]) {  │
    │   │     打印取消信息             │
    │   │ }                            │
    │   └─────────────────────────────┘
    │
    └─ 队伍3（Switzerland）
        ↓
        ┌─────────────────────────────┐
        │ 打印成绩                     │
        │ Team Switzerland = 42.3 s    │
        └─────────────────────────────┘
        ↓
        ┌─────────────────────────────┐
        │ 检查是否被取消资格           │
        │ if (teamDisqualified[3]) {  │
        │     打印取消信息             │
        │ }                            │
        └─────────────────────────────┘
    ↓
┌─────────────────────────────────────┐
│ 打印空行                             │
│ std::cout << std::endl;              │
└─────────────────────────────────────┘
    ↓
return 0;

============================================
关键点：
============================================

1. 位置5在main函数的最后
   - 所有线程都join完成后
   - 在return 0之前

2. 遍历所有队伍
   - 打印每个队伍的成绩
   - 检查每个队伍是否被取消资格

3. 显示取消资格信息
   - 如果teamDisqualified[i]是true
   - 打印取消资格的信息

4. 最后添加空行
   - 让输出更美观
   - std::cout << std::endl;
